# Import Samples

Use these files to manually test the import flow in the web interface.

- `sample-curl.txt` for curl import
- `sample-openapi.json` for OpenAPI import

Both files are intentionally small and valid so you can paste them into the importer or load them from a file picker if the UI supports it.